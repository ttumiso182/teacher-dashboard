specVersion: "v1beta"
serviceId: "teacher-dashboard"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "teacher-dashboard-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
